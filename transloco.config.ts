import { TranslocoGlobalConfig } from '@jsverse/transloco-utils';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'apps/barbershop-management/public/i18n/',
  langs: ['pt-BR', 'en'],
  keysManager: {},
};

export default config;
